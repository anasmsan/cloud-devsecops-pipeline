.venv-tools/
.venv-app/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
node_modules/
.gitlab-ci-local/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
# Contient une IP personnelle (admin_cidr_blocks) : chacun crée le sien à partir
# de terraform.tfvars.example plutôt que de partager une valeur personnelle.
terraform.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Kubeconfig téléchargé localement (contient un certificat client, jamais versionné)
kubeconfig*
